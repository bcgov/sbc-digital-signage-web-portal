[project]
name = "sbc-digital-signage-web-portal"
version = "0.1.0"
description = "Web portal to manage digital signage displayed in Service BC offices"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "flask>=2.2,<2.3",
    "python-dotenv==0.21.1",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "ruff>=0.15.12",
]
